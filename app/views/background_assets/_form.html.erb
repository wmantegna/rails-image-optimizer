<%= form_for(@background_asset) do |f| %>
  <% if @background_asset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@background_asset.errors.count, "error") %> prohibited this background_asset from being saved:</h2>

      <ul>
      <% @background_asset.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.file_field :attachment, as: :file %>
<br />
<br /><br /><br /><br />
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
